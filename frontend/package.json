{
  "name": "expo-speech-app",
  "version": "1.0.0",
  "main": "node_modules/expo/AppEntry.js",
  "scripts": {
    "start": "expo start",
    "android": "expo run:android",
    "ios": "expo run:ios",
    "web": "expo start --web"
  },
  "dependencies": {
    "expo": "~50.0.0",                            // ← 請依實際版本調整
    "expo-dev-client": "^2.0.1",                  // ← EAS 開發用 client
    "react": "18.2.0",
    "react-native": "0.73.0",
    "@react-native-voice/voice": "^3.2.2"         // ← 語音辨識套件
  },
  "devDependencies": {
    "@babel/core": "^7.20.0"
  },
  "private": true
}
